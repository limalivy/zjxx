# 字界修仙 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a browser-based Chinese character typing game with cultivation theme — characters fall, player types to eliminate them, mistakes cost HP.

**Architecture:** Three standalone files (HTML, CSS, JS) with no frameworks. Game loop driven by requestAnimationFrame at 60fps. Input handled via IME-aware composition events with real-time matching. Character spawning uses hybrid timer + on-demand model.

**Tech Stack:** Vanilla HTML5 + CSS3 + ES6 JavaScript. No dependencies.

---

### Task 1: Create HTML structure

**Files:**
- Create: `index.html`

- [ ] **Step 1: Write the complete HTML skeleton**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>字界修仙</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <!-- 游戏主容器 -->
  <div id="game-container">
    <!-- HUD：标题、血条、分数 -->
    <div id="hud">
      <h1 id="title">字 界 修 仙</h1>
      <div id="hp-bar-container">
        <div id="hp-bar-fill"></div>
        <span id="hp-text">50 / 50</span>
      </div>
      <div id="score-display">得分: <span id="score-value">0</span></div>
    </div>

    <!-- 掉落字区域 -->
    <div id="char-area"></div>

    <!-- 底部输入区 -->
    <div id="input-area">
      <input
        type="text"
        id="char-input"
        autocomplete="off"
        autofocus
        placeholder="在此输入..."
      >
    </div>

    <!-- 全屏闪烁遮罩 -->
    <div id="flash-overlay"></div>

    <!-- 游戏结束遮罩 -->
    <div id="gameover-overlay" class="hidden">
      <div id="gameover-box">
        <h2>道心破碎</h2>
        <p id="final-score"></p>
        <button id="restart-btn">重新修仙</button>
      </div>
    </div>
  </div>

  <script src="game.js"></script>
</body>
</html>
```

- [ ] **Step 2: Open index.html in browser to verify structure renders**

Open `index.html` in browser. Expected: blank dark page with an input box at bottom and title at top. No functionality yet.

---

### Task 2: Create CSS stylesheet

**Files:**
- Create: `style.css`

- [ ] **Step 1: Write complete CSS — reset, layout, HUD**

```css
/* ===== Reset & Base ===== */
*, *::before, *::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html, body {
  width: 100%;
  height: 100%;
  overflow: hidden;
  font-family: KaiTi, STKaiti, "楷体", "Noto Serif SC", serif;
  background: #0d0d1a;
  color: #d4a574;
  user-select: none;
}

/* ===== 主容器 ===== */
#game-container {
  position: relative;
  width: 100%;
  height: 100%;
  background: radial-gradient(ellipse at 50% 30%, #1a1a3e 0%, #0d0d1a 70%);
}

/* ===== HUD ===== */
#hud {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  z-index: 10;
  padding: 16px 24px;
  background: linear-gradient(180deg, rgba(13,13,26,0.95) 60%, rgba(13,13,26,0) 100%);
  pointer-events: none;
}

#title {
  text-align: center;
  font-size: 2rem;
  letter-spacing: 0.5em;
  color: #ffd700;
  text-shadow: 0 0 20px rgba(255, 215, 0, 0.4);
  margin-bottom: 12px;
}

/* HP 血条 */
#hp-bar-container {
  position: relative;
  width: 100%;
  max-width: 400px;
  height: 22px;
  margin: 0 auto 8px;
  background: rgba(0,0,0,0.5);
  border: 1px solid #d4a574;
  border-radius: 4px;
  overflow: hidden;
}

#hp-bar-fill {
  height: 100%;
  width: 100%;
  background: linear-gradient(90deg, #4caf50, #8bc34a, #ffeb3b, #ff9800, #f44336);
  background-size: 400% 100%;
  background-position: 0% 50%;
  transition: width 0.3s ease, background-position 0.3s ease;
  border-radius: 3px;
}

#hp-text {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  font-size: 0.75rem;
  color: #fff;
  text-shadow: 0 0 4px rgba(0,0,0,0.8);
}

#score-display {
  text-align: center;
  font-size: 1rem;
  color: #d4a574;
}

#score-value {
  color: #ffd700;
  font-size: 1.2rem;
}
```

- [ ] **Step 2: Write CSS — character area and falling chars**

```css
/* ===== 掉落字区域 ===== */
#char-area {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
  z-index: 5;
}

.falling-char {
  position: absolute;
  font-size: 2.5rem;
  color: #ffd700;
  text-shadow: 0 0 12px rgba(255, 215, 0, 0.5), 0 0 24px rgba(255, 215, 0, 0.2);
  transition: opacity 0.3s ease, transform 0.3s ease;
  animation: charGlow 2s ease-in-out infinite alternate;
}

@keyframes charGlow {
  0%   { text-shadow: 0 0 12px rgba(255,215,0,0.5), 0 0 24px rgba(255,215,0,0.2); }
  100% { text-shadow: 0 0 20px rgba(255,215,0,0.8), 0 0 40px rgba(255,215,0,0.4); }
}

/* 消除动画 */
.falling-char.popping {
  animation: charPop 0.3s ease-out forwards;
}

@keyframes charPop {
  0%   { transform: scale(1); opacity: 1; }
  50%  { transform: scale(1.5); opacity: 0.7; color: #fff; }
  100% { transform: scale(0); opacity: 0; }
}
```

- [ ] **Step 3: Write CSS — input area**

```css
/* ===== 输入区 ===== */
#input-area {
  position: absolute;
  bottom: 20px;
  left: 0;
  right: 0;
  z-index: 20;
  display: flex;
  justify-content: center;
}

#char-input {
  width: 280px;
  padding: 12px 20px;
  font-size: 1.2rem;
  font-family: KaiTi, STKaiti, "楷体", "Noto Serif SC", serif;
  text-align: center;
  background: rgba(255,255,255,0.08);
  border: 1px solid #d4a574;
  border-radius: 8px;
  color: #ffd700;
  outline: none;
  transition: border-color 0.2s, box-shadow 0.2s;
}

#char-input::placeholder {
  color: rgba(212, 165, 116, 0.4);
}

#char-input:focus {
  border-color: #ffd700;
  box-shadow: 0 0 16px rgba(255, 215, 0, 0.3);
}
```

- [ ] **Step 4: Write CSS — flash overlay and game over**

```css
/* ===== 全屏闪烁 ===== */
#flash-overlay {
  position: fixed;
  top: 0; left: 0;
  width: 100%; height: 100%;
  background: rgba(220, 20, 60, 0.35);
  z-index: 50;
  pointer-events: none;
  opacity: 0;
  transition: opacity 0ms;
}

#flash-overlay.active {
  opacity: 1;
  transition: opacity 0ms;
}

#flash-overlay.fading {
  opacity: 0;
  transition: opacity 300ms ease-out;
}

/* ===== 游戏结束遮罩 ===== */
#gameover-overlay {
  position: fixed;
  top: 0; left: 0;
  width: 100%; height: 100%;
  background: rgba(0, 0, 0, 0.75);
  z-index: 100;
  display: flex;
  align-items: center;
  justify-content: center;
}

#gameover-overlay.hidden {
  display: none;
}

#gameover-box {
  text-align: center;
  padding: 48px 64px;
  background: rgba(26, 26, 62, 0.95);
  border: 2px solid #d4a574;
  border-radius: 16px;
  box-shadow: 0 0 60px rgba(212, 165, 116, 0.2);
}

#gameover-box h2 {
  font-size: 2.5rem;
  color: #f44336;
  margin-bottom: 20px;
  letter-spacing: 0.3em;
}

#gameover-box p {
  font-size: 1.3rem;
  color: #ffd700;
  margin-bottom: 30px;
}

#restart-btn {
  padding: 12px 40px;
  font-size: 1.2rem;
  font-family: KaiTi, STKaiti, "楷体", serif;
  color: #0d0d1a;
  background: linear-gradient(135deg, #ffd700, #d4a574);
  border: none;
  border-radius: 8px;
  cursor: pointer;
  transition: transform 0.15s, box-shadow 0.15s;
}

#restart-btn:hover {
  transform: scale(1.05);
  box-shadow: 0 0 24px rgba(255, 215, 0, 0.4);
}

#restart-btn:active {
  transform: scale(0.98);
}
```

- [ ] **Step 5: Open index.html in browser to verify styles**

Expected: Dark full-screen page with gradient background, gold title "字 界 修 仙" at top, HP bar with green gradient, score display, input box at bottom with gold border. No functionality yet.

---

### Task 3: Create game.js — word bank and state

**Files:**
- Create: `game.js`

- [ ] **Step 1: Write word bank (200 common Chinese characters)**

```js
// ===== 字库：200 个常用汉字 =====
const CHAR_BANK = [
  '的','一','是','不','了','人','我','在','有','他',
  '这','中','大','来','上','国','个','到','说','们',
  '为','子','和','你','地','出','道','也','时','年',
  '得','就','那','要','下','以','生','会','自','着',
  '过','家','学','对','可','她','里','后','小','么',
  '心','多','天','而','能','好','都','然','没','日',
  '于','起','还','发','成','事','只','作','当','想',
  '看','文','无','开','手','十','用','主','行','方',
  '又','如','前','所','本','见','经','头','面','公',
  '同','三','已','老','从','动','两','长','知','民',
  '样','现','分','将','外','但','身','些','与','高',
  '意','进','把','法','此','实','回','二','理','美',
  '点','月','明','其','种','声','全','工','己','话',
  '正','治','战','体','水','力','军','很','名','使',
  '重','者','山','利','相','度','新','物','气','间',
  '并','关','比','或','最','通','系','问','制','向',
  '业','门','应','加','量','平','命','提','机','它',
  '第','展','己','去','电','教','少','各','代','百',
  '世','处','风','路','常','内','被','西','白','光',
  '金','具','安','合','变','口','先','打','花','觉'
];
```

- [ ] **Step 2: Write game state and DOM refs**

```js
// ===== DOM 引用 =====
const charArea = document.getElementById('char-area');
const charInput = document.getElementById('char-input');
const hpBarFill = document.getElementById('hp-bar-fill');
const hpText = document.getElementById('hp-text');
const scoreValue = document.getElementById('score-value');
const flashOverlay = document.getElementById('flash-overlay');
const gameoverOverlay = document.getElementById('gameover-overlay');
const finalScore = document.getElementById('final-score');
const restartBtn = document.getElementById('restart-btn');

// ===== 游戏状态 =====
const state = {
  hp: 50,
  maxHp: 50,
  score: 0,
  isGameOver: false,
  activeChars: [],       // { char, x, y, speed, el }
  spawnTimerId: null,
  animFrameId: null,
  isComposing: false,
};
```

- [ ] **Step 3: Verify file is valid JS**

Test with `node --check game.js` (if Node available) or verify no syntax errors in browser console after loading.

---

### Task 4: Create game.js — character spawning

**Files:**
- Modify: `game.js` (append to existing)

- [ ] **Step 1: Write spawn function**

```js
// ===== 字库索引（循环使用） =====
let charBankIndex = 0;
const shuffledBank = [...CHAR_BANK].sort(() => Math.random() - 0.5);

/** 获取下一个字（打乱后循环取用） */
function nextChar() {
  const ch = shuffledBank[charBankIndex];
  charBankIndex = (charBankIndex + 1) % shuffledBank.length;
  return ch;
}

/** 生成一个掉落字 */
function spawnChar() {
  if (state.isGameOver) return;
  if (state.activeChars.length >= 5) return;

  const char = nextChar();
  const x = 40 + Math.random() * (window.innerWidth - 120);
  const y = -(30 + Math.random() * 90);   // -30 ~ -120
  const speed = 0.5 + Math.random() * 0.7; // 0.5 ~ 1.2 px/frame

  const el = document.createElement('span');
  el.className = 'falling-char';
  el.textContent = char;
  el.style.left = x + 'px';
  el.style.top = y + 'px';
  charArea.appendChild(el);

  state.activeChars.push({ char, x, y, speed, el });
}

/** 开始定时刷新 */
function startSpawning() {
  state.spawnTimerId = setInterval(() => {
    spawnChar();
  }, 2500);
}

/** 停止定时刷新 */
function stopSpawning() {
  if (state.spawnTimerId) {
    clearInterval(state.spawnTimerId);
    state.spawnTimerId = null;
  }
}
```

- [ ] **Step 2: Verify by adding a test call**

Add temporary `spawnChar();` call and check browser console: one character element should appear in `#char-area`.

---

### Task 5: Create game.js — game loop

**Files:**
- Modify: `game.js` (append to existing)

- [ ] **Step 1: Write game loop function**

```js
// ===== 游戏循环 =====
function gameLoop() {
  if (state.isGameOver) return;

  const screenBottom = window.innerHeight;

  // 倒序遍历（方便删除）
  for (let i = state.activeChars.length - 1; i >= 0; i--) {
    const ch = state.activeChars[i];
    ch.y += ch.speed;
    ch.el.style.top = ch.y + 'px';

    // 检测越界
    if (ch.y > screenBottom) {
      removeChar(i);
      takeDamage(2);
    }
  }

  state.animFrameId = requestAnimationFrame(gameLoop);
}

/** 移除指定索引的字（不扣血） */
function removeChar(index) {
  const ch = state.activeChars[index];
  ch.el.remove();
  state.activeChars.splice(index, 1);
}

/** 启动游戏循环 */
function startLoop() {
  state.animFrameId = requestAnimationFrame(gameLoop);
}

/** 停止游戏循环 */
function stopLoop() {
  if (state.animFrameId) {
    cancelAnimationFrame(state.animFrameId);
    state.animFrameId = null;
  }
}
```

---

### Task 6: Create game.js — HP management and flash

**Files:**
- Modify: `game.js` (append to existing)

- [ ] **Step 1: Write HP management functions**

```js
// ===== HP 管理 =====
function takeDamage(amount) {
  if (state.isGameOver) return;

  state.hp = Math.max(0, state.hp - amount);
  updateHpBar();
  triggerFlash();

  if (state.hp <= 0) {
    gameOver();
  }
}

function updateHpBar() {
  const pct = (state.hp / state.maxHp) * 100;
  hpBarFill.style.width = pct + '%';
  // 血条渐变位置随血量变化（绿→红）
  hpBarFill.style.backgroundPosition = (100 - pct) + '% 50%';
  hpText.textContent = state.hp + ' / ' + state.maxHp;
}

// ===== 全屏闪烁 =====
function triggerFlash() {
  // 重置：移除 fading class，添加 active（立即红色）
  flashOverlay.classList.remove('fading');
  flashOverlay.classList.add('active');

  // 下一帧开始淡出
  requestAnimationFrame(() => {
    flashOverlay.classList.remove('active');
    flashOverlay.classList.add('fading');
  });
}
```

---

### Task 7: Create game.js — input handling with IME

**Files:**
- Modify: `game.js` (append to existing)

- [ ] **Step 1: Write input handling functions**

```js
// ===== 输入处理 =====
charInput.addEventListener('compositionstart', () => {
  state.isComposing = true;
});

charInput.addEventListener('compositionend', (e) => {
  state.isComposing = false;
  // IME 组合完成，取最终输入
  const text = e.data || charInput.value;
  if (text) {
    processInput(text);
  }
  charInput.value = '';
});

charInput.addEventListener('input', () => {
  // 非 IME 输入时直接处理（如直接输入英文、数字）
  if (!state.isComposing) {
    const text = charInput.value;
    if (text) {
      processInput(text);
      charInput.value = '';
    }
  }
});

/** 处理输入文本，匹配屏幕上的字 */
function processInput(text) {
  if (state.isGameOver) return;

  // 取最后一个有效字符（兼容输入法可能带的多余字符）
  const inputChar = text.slice(-1);
  if (!inputChar) return;

  // 在活跃字中查找匹配
  let matched = false;
  for (let i = state.activeChars.length - 1; i >= 0; i--) {
    if (state.activeChars[i].char === inputChar) {
      // 匹配成功！
      popChar(i);
      state.score += 10;
      scoreValue.textContent = state.score;
      spawnChar(); // 打掉即补
      matched = true;
      break;
    }
  }

  if (!matched) {
    // 匹配失败
    takeDamage(1);
  }
}

/** 消除字（带动画） */
function popChar(index) {
  const ch = state.activeChars[index];
  ch.el.classList.add('popping');

  // 动画结束后移除
  ch.el.addEventListener('animationend', function handler() {
    ch.el.removeEventListener('animationend', handler);
    ch.el.remove();
  }, { once: true });

  state.activeChars.splice(index, 1);
}
```

---

### Task 8: Create game.js — game over logic

**Files:**
- Modify: `game.js` (append to existing)

- [ ] **Step 1: Write game over and restart functions**

```js
// ===== 游戏结束 =====
function gameOver() {
  state.isGameOver = true;
  stopLoop();
  stopSpawning();

  // 清除所有掉落字
  for (const ch of state.activeChars) {
    ch.el.remove();
  }
  state.activeChars = [];

  // 禁用输入
  charInput.disabled = true;

  // 显示结束界面
  finalScore.textContent = '最终得分: ' + state.score;
  gameoverOverlay.classList.remove('hidden');
}

// ===== 重新开始 =====
function restart() {
  // 重置状态
  state.hp = state.maxHp;
  state.score = 0;
  state.isGameOver = false;
  state.activeChars = [];
  state.isComposing = false;

  // 重置 UI
  updateHpBar();
  scoreValue.textContent = '0';
  charInput.disabled = false;
  charInput.value = '';
  gameoverOverlay.classList.add('hidden');

  // 清除残留 DOM
  charArea.innerHTML = '';

  // 重新打乱字库
  shuffledBank.sort(() => Math.random() - 0.5);
  charBankIndex = 0;

  // 启动游戏
  startSpawning();
  startLoop();
  charInput.focus();
}

restartBtn.addEventListener('click', restart);
```

---

### Task 9: Create game.js — initialization

**Files:**
- Modify: `game.js` (append to existing)

- [ ] **Step 1: Write init function and call it**

```js
// ===== 游戏初始化 =====
function init() {
  // 初始生成 3 个字，让游戏一开始就有东西
  spawnChar();
  spawnChar();
  spawnChar();

  // 启动定时刷新和游戏循环
  startSpawning();
  startLoop();

  // 聚焦输入框
  charInput.focus();

  // 保持输入框始终聚焦
  document.addEventListener('click', () => {
    if (!state.isGameOver) {
      charInput.focus();
    }
  });
}

// 启动！
init();
```

- [ ] **Step 2: Add window resize handler (optional polish)**

```js
// 窗口大小变化时，确保字不会超出边界
window.addEventListener('resize', () => {
  const maxX = window.innerWidth - 100;
  for (const ch of state.activeChars) {
    if (ch.x > maxX) {
      ch.x = maxX;
      ch.el.style.left = maxX + 'px';
    }
  }
});
```

---

### Task 10: Integration verification

- [ ] **Step 1: Open index.html in browser**

- [ ] **Step 2: Verify the following behaviors:**

| 检查项 | 预期 |
|--------|------|
| 页面加载后 | 3 个字出现并开始下落 |
| 输入正确字 | 字播放消除动画消失，+10 分，立即补充新字 |
| 输入错误字 | 全屏红色闪烁，-1 HP，血条缩短 |
| 字掉到底部 | -2 HP，血条缩短，全屏红色闪烁 |
| HP ≤ 0 | 显示"道心破碎"结束界面，显示最终得分 |
| 点击"重新修仙" | 游戏完全重置，重新开始 |
| 定时刷新 | 每 2.5 秒出现新字（屏幕不足 5 个时） |
| 屏幕最多 5 字 | 始终不超过 5 个 |
| 输入框始终聚焦 | 点击页面任意位置后输入框恢复焦点 |

- [ ] **Step 3: Test with Chinese IME**

Switch to Chinese input method (拼音/五笔等), verify:
- 拼音输入过程中不会触发匹配
- 选字确认后正确触发匹配（匹配成功 → 消字，失败 → 扣血）

- [ ] **Step 4: Play a full game from start to game over**

Self-test: play until HP reaches 0, verify flow is smooth and all mechanics work.

---

### Notes

- There are no unit tests for this project (UI-heavy game). Verification is manual browser testing as described in Task 10.
- The game is a single HTML file referencing external CSS and JS — open `index.html` directly in browser (no server required).
- If you want to serve via localhost for any reason: `npx serve .` or `python -m http.server 8000` in the project root.
- Speed and spawn interval values (0.5-1.2 px/frame, 2.5s) are starting points — tune after playtesting.
